name: fish_counter
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  battery_plus: ^7.0.0
  firebase_core: ^4.3.0
  sensors_plus: ^7.0.0
  share_plus: ^13.1.0
  weather: ^3.2.1
  geolocator: ^14.0.2
  http: ^1.6.0
  firebase_auth: ^6.5.2
  google_sign_in: ^7.2.0
  cloud_firestore: ^6.5.0
  provider: ^6.1.1
  crypto: ^3.0.7
  hive_ce: ^2.19.3
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  firebase_auth_mocks: ^0.15.2

flutter:
  uses-material-design: true
  assets:
    - assets/branding/app_logo.png
    - assets/branding/app_store_preview_square_1024.png
