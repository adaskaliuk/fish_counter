# session-edit-dialog / Діалог редагування сесії

- Hide coach-only session fields in `lib/widgets/session_edit_dialog.dart`.
- Show `coachComment` only in coach mode.
- Keep athlete note visible where applicable.
